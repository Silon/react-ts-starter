import { RootState } from 'src/redux/store';
import { T{{pascalCase name}}State } from 'src/redux/{{camelCase name}}/{{camelCase name}}.slice';

export const selectExample = (state: RootState): T{{pascalCase name}}State['example'] => state.{{camelCase name}}.example;
